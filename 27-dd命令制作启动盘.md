## 🧭 一、原理简述
`dd` 是一个“**按字节复制**”的命令，可以把 ISO 镜像文件直接写入到 U 盘设备上，  
从而让 U 盘具备启动系统的引导扇区结构。

简单说，它就是：

🧱 “把 ISO 文件的原始内容原封不动地拷贝进U盘”。

---

## 🧩 二、操作步骤
### ✅ 步骤 1：插入 U 盘，查看设备名
执行：

```plain
lsblk
```

输出示例：

```plain
NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
sda      8:0    0  100G  0 disk 
├─sda1   8:1    0   50G  0 part /
├─sda2   8:2    0   50G  0 part /data
sdb      8:16   1   16G  0 disk 
└─sdb1   8:17   1   16G  0 part /run/media/user/USB
```

→ 可以看出你的 **U 盘是 **`**/dev/sdb**`。

---

### ✅ 步骤 2：卸载 U 盘分区
在写入前，必须先卸载挂载点：

```plain
umount /dev/sdb*
```

---

### ✅ 步骤 3：使用 `dd` 写入镜像
假设你的镜像文件是 `rhel-8.10-x86_64-dvd.iso`，执行：

```plain
sudo dd if=/path/to/rhel-8.10-x86_64-dvd.iso of=/dev/sdb bs=4M status=progress oflag=sync
```

参数解释：

+ `if=` ：输入文件（input file）
+ `of=` ：输出目标（output file） → 一定要是设备 `/dev/sdb`，**不是分区 **`**/dev/sdb1**`
+ `bs=4M` ：每次写 4MB，加快速度
+ `status=progress` ：显示写入进度
+ `oflag=sync` ：确保每次写完都同步到设备，防止缓存没刷盘

---

### ✅ 步骤 4：等待完成并同步缓存
写完后执行：

```plain
sync
```

确保数据真正落盘。

---

### ✅ 步骤 5：验证
重新插拔U盘，然后执行：

```plain
lsblk -f
```

或：

```plain
file -s /dev/sdb
```

如果输出类似：

```plain
/dev/sdb: ISO 9660 CD-ROM filesystem data 'RHEL-8-10-x86_64-DVD'
```

说明启动盘写入成功。

---

## ⚠️ 三、常见注意事项
| ⚠️ 事项 | 说明 |
| --- | --- |
| ❌ 不要写错设备 | 一旦写错 `/dev/sda`<br/> → 整个系统盘数据会被抹掉 |
| ⚙️ 建议确认命令 | 可先用 `echo`<br/> 模拟输出，例如：`echo dd if=xxx of=/dev/sdb` |
| ⏱️ 写入时间 | 视镜像大小与U盘速度而定（约 5~10 分钟） |
| 🧹 清除U盘后重用 | 可以用 `sudo wipefs -a /dev/sdb`<br/> 或 `sudo dd if=/dev/zero of=/dev/sdb bs=1M count=10`<br/> 清空启动信息 |


---

## 🧠 四、额外技巧
### 1. 查看写入速度实时进度
如果系统的 `dd` 版本较旧没有 `status=progress`，可以用：

```plain
sudo dd if=xxx.iso of=/dev/sdb bs=4M &
sudo pkill -USR1 -n -x dd
```

---

### 2. 把启动盘内容挂载查看
```plain
mkdir /mnt/iso
mount /dev/sdb /mnt/iso
ls /mnt/iso
```

可以看到镜像文件内容（验证成功）。

---

## ✅ 五、总结一句话
| 目的 | 命令 | 备注 |
| --- | --- | --- |
| 写入启动盘 | `sudo dd if=xxx.iso of=/dev/sdX bs=4M status=progress oflag=sync` | ⚠️ 确认 `sdX`<br/> 正确 |
| 查看设备 | `lsblk` | 查U盘名 |
| 同步缓存 | `sync` | 确保数据落盘 |


